server:
  port: 8761
eureka:
  client:
    register-with-eureka: false  # 服务不注册
    fetch-registry: false        # 服务不注册
    service-url:
      defaultZone: http://localhost:8761/eureka
  server:
    enable-self-preservation: false # 关闭自我保护

